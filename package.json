{
  "name": "maple",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "lib/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev --ui=tui",
    "dev:alerting": "bun --filter=@maple/alerting dev",
    "alchemy:deploy:prd": "bun run --filter=@maple/api deploy:stack -- --stage prd && bun run --filter=@maple/web deploy:stack -- --stage prd && bun run --filter=@maple/landing deploy:stack -- --stage prd",
    "alchemy:deploy:stg": "bun run --filter=@maple/api deploy:stack -- --stage stg && bun run --filter=@maple/web deploy:stack -- --stage stg && bun run --filter=@maple/landing deploy:stack -- --stage stg",
    "alchemy:deploy:pr": "bun run --filter=@maple/api deploy:stack -- --stage pr-${PR_NUMBER} && bun run --filter=@maple/web deploy:stack -- --stage pr-${PR_NUMBER} && bun run --filter=@maple/landing deploy:stack -- --stage pr-${PR_NUMBER}",
    "alchemy:destroy:prd": "bun run --filter=@maple/landing destroy:stack -- --stage prd && bun run --filter=@maple/web destroy:stack -- --stage prd && bun run --filter=@maple/api destroy:stack -- --stage prd",
    "alchemy:destroy:stg": "bun run --filter=@maple/landing destroy:stack -- --stage stg && bun run --filter=@maple/web destroy:stack -- --stage stg && bun run --filter=@maple/api destroy:stack -- --stage stg",
    "alchemy:destroy:pr": "bun run --filter=@maple/landing destroy:stack -- --stage pr-${PR_NUMBER} && bun run --filter=@maple/web destroy:stack -- --stage pr-${PR_NUMBER} && bun run --filter=@maple/api destroy:stack -- --stage pr-${PR_NUMBER}",
    "test": "turbo test",
    "tinybird:manifest": "bun run ./scripts/generate-tinybird-project-manifest.ts",
    "tinybird:manifest:check": "bun run ./scripts/generate-tinybird-project-manifest.ts --check",
    "typecheck": "turbo typecheck"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "4.20260414.1",
    "@maple/db": "workspace:*",
    "@tinybirdco/sdk": "catalog:tinybird",
    "turbo": "^2.9.3",
    "typescript": "catalog:tooling"
  },
  "packageManager": "bun@1.3.11",
  "catalog": {
    "vite": "^8.0.3",
    "vitest": "^4.1.2"
  },
  "catalogs": {
    "effect": {
      "@effect/atom-react": "4.0.0-beta.47",
      "@effect/language-service": "^0.85.1",
      "@effect/opentelemetry": "4.0.0-beta.47",
      "@effect/platform-bun": "4.0.0-beta.47",
      "effect": "4.0.0-beta.47"
    },
    "react": {
      "@types/react": "^19.2.14",
      "@types/react-dom": "^19.2.3",
      "react": "^19.2.4",
      "react-dom": "^19.2.4"
    },
    "tailwind": {
      "@tailwindcss/vite": "^4.2.2",
      "tailwind-merge": "^3.5.0",
      "tailwindcss": "^4.2.2",
      "tw-animate-css": "^1.4.0"
    },
    "tinybird": {
      "@tinybirdco/sdk": "0.0.64"
    },
    "tooling": {
      "@types/node": "^22.10.2",
      "typescript": "^6.0.2"
    },
    "ui": {
      "@base-ui/react": "^1.3.0",
      "@xyflow/react": "^12.10.2",
      "class-variance-authority": "^0.7.1",
      "clsx": "^2.1.1",
      "recharts": "2.15.4"
    }
  },
  "dependencies": {}
}
